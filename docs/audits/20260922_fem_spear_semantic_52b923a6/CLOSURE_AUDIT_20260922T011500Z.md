# Closure audit — FEM SPEAR semantic rank identity 52b923a6

LANGUAGE=EN
RUN_ID: 20260922T011500Z
CONTRADICTION_FOUND: NO

```text
IDENTITY=52b923a6a2ccfba714cd27fddb9955705844203e862747479e2f2f8895eae08a
BOARD_PASS=NO
PROGRAM_PASS=NO
FEM_PERSIST_PASS=NO
TIMING_PASS=NO
MIG_PASS=NO
PACK_ABI_24_24_PASS=NO
PROGRAM.DONE=NA
LIVE_SRAM_HASH=NOT_READ
C_RTL_EDIT=NO
```

Bit rehashed this turn. Disk copies `3ccd03f8…` and `1db38691…` still match. Synth reads `fem_spear_semantic/arty_a7_r2_top_m4_mig_candidate.sv`. End of startup HIGH. `PROGRAM.DONE` was not readable.

## Alternate writer

`cand_desc` is assigned only from `DESC_A` and `DESC_B` inside `spear_fem_rank`. Those are localparams. `SRNK` uses `arg[0]` as `fem_infl_en` and ignores the rest of the argument. No opcode carries a descriptor, a query, or a generation. There is no second `spear_rank`. Q* and DEST_POKE are not on this capture (`QARM`, `QTHW`, and `44504B31` are absent).

Query `namespace=0x0011`, `generation=0x0007`, `relation=3` are constants in the same module, not ports.

## Invariance on the wire

| Arm | rank0 | base A/B | delta B | final A/B | ft | life | comp | infl |
|---|---|---|---:|---|---:|---:|---:|---:|
| OFF1 | `0xA1` | 127 / 0 | 0 | 127 / 0 | 2 | 3 | 1 | 0 |
| ON1 | `0xB1` | 127 / 0 | 256 | 127 / 256 | 2 | 3 | 1 | 1 |
| OFF2 | `0xA1` | 127 / 0 | 0 | 127 / 0 | 2 | 3 | 1 | 0 |
| ON2 | `0xB1` | 127 / 0 | 256 | 127 / 256 | 2 | 3 | 1 | 1 |

`0xA1` and `0xB1` are the refs inside the two localparams. `256 = failure_total<<7` with `failure_total=2`. The four frames differ, so the rank is not a stuck latch. The echo is sent only from `S_WAIT` after `rank_done`, in the same frame as that arm's `infl` bit.

`fem_ft` into the delta is the synced FEM snapshot `ft_s1`, sampled when the arm starts. FOBS after FREC already showed life 3 and ft 2, and no FEM opcode sits between the four `SRNK` steps.

The 128-bit descriptors are not echoed. Invariance of those bytes is the RTL localparam plus the matching refs and base scores, not a descriptor readback.

## Verdict

No alternate writer and no arm/rank mismatch. The scoped rank claim can be frozen. This bit still does not turn that rank into a different final action through ASTRA or Q*.
