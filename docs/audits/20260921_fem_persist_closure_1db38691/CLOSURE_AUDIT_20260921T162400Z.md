# Independent closure audit — FEM legal-compact board candidate

LANGUAGE=EN  
RUN_ID: 20260921T162400Z  
AUDITOR: CURSOR_OWNER (this turn; does not inherit D self-stamp)  
AUTHORITY: `07fa414555a7c729cc4d4e4dd093a35a03ea2aa6`  
IDENTITY (programmed SHA + no-reprogram provenance + unique FEM UART; **not** live SRAM hash):  
`1db38691530304e929b437774ebba9a9122590d0a38685a0bba2f646c5b56668`  
C_RTL: `fem_lifecycle.v` sha256 `45b9b93073e3e08a2525a3fc1a96573d750bedcd26570cebefba5fa9514779ed` **unedited**  
JSON: `UART_LEGAL_COMPACT.json` sha256 `6378acafe72067f208ba2aae1d324a27bce3f5953cb21188f7275b3d8f34f13f`  
HARNESS: `uart_fem_persist_legal_compact.py` sha256 `f6da7ae0d3d1fea11ce2d3d2d3d47b3a4a748cd8c3d031a04ce3cab1866b2bbe`

```text
C_RTL_EDIT=NO
NEW_BITSTREAM=NO
REPROGRAM_THIS_AUDIT=NO
DEST_POKE=NO
RED_RESET=NO
FEM_PERSIST_PASS=NO
PROGRAM_PASS=NO
BOARD_PASS=NO
MIG_PASS=NO
TIMING_PASS=NO
PACK_ABI_24_24_PASS=NO
PROGRAM.DONE=NA
LIVE_SRAM_HASH=NOT_READ
```

Inquiry depth: Level 3 (closure freeze). Historical no-FREP DEST_READ-first files remain append-only under `SUPERSEDED_BY_CAUSAL_AUDIT`.

---

## Dataset under audit

Legal-compact UART on COM12, 2026-09-21T23:09:00..04+07, same programmed identity, no new bit.

Gated sequence (FOBS, not opcode echo):

CLEAR ACK `c1ea50a5` → FEM-only FRST → FOBS virgin (life=7 n_raw=0 key=0) → FING 5/6/4/4 → FOBS L_CLUSTERED n_raw=2 key=0x70ea → FREP 0x0111 ×3 → FOBS L_RESOLVED sar=1,2,3 → FCMP → FOBS cmp_result=0 life=3 compacted=1 n_raw=0 ft=2 fr=0 sar=3 key=0x70ea → DEST_READ `0x0200000` / `0x0200010` → FRST → FOBS virgin → DEST_READ identical → FREC → FOBS recov=2 life=3 compacted=1 integrity_fault=0 → DEST_READ `0x0200010` still `c0117ed0`.

Independent CRC16/CCITT-FALSE over `{0x70ea0203,0x11010000}` = `0x552e`. Observed A_CRCW = `a5a5552e`. Recomputed this audit.

JSON contains eight `c0117ed0` strings and **no** `44504B31` / DPK1.

---

## Falsification search (owner checklist)

### 1. Alternate writers of FEM_BASE

| Path | Can write FEM_BASE? | Fired this run? |
|---|---|---|
| C `fem_lifecycle` T2 (`fem_on_mig` → `fem_t2_ce` → `fem_t2_adapter` → `fem_req_ui` `FEM_BASE+{addr,2'b00}` → `mig_ui32`) | YES (legal lifecycle) | YES after FCMP (HDR/W0/W1/CRCW/COMMIT/INDEX/HDR2 match C packing) |
| `dest_diag_ui` via UART `DPK1` | YES (arbitrary 128-bit beat) | **NO** — harness has no DPK opcode; JSON `dest_poke=NO`; no `44504B31` in capture |
| Pack `pack_loader` SLOT0=`0` SLOT1=`28'h010_0000` | dest at those bases | **NO alias** with FEM_BASE `28'h0200000` (addr[21] vs addr[20]) |
| Host UART reconstruction of DEST_READ lanes | decode only | **NO** — `raw_hex` LE words match lanes (`31445244 d07e11c0 01000000 ea700111 00000100` → `c0117ed0 00000001 110170ea 00010000`) |
| Unique persist SPEAR/Q* | idle, not a dest writer | `q_start=0` `cand_valid=0` `prop_start=0` `feat_flat=0` |

**FACT:** In synthesizable C FEM, `t2_wdata <= COMMIT_MAGIC` exists only at `C_COMMIT`. `fem_commit_class.v` classifies; it does not write T2. Unique-tree TBs compare magic; they are not on silicon.

### 2. Path capable of manufacturing `C0117ED0`

Silicon writers: C_COMMIT (legal) and dest_diag DEST_POKE (illegal for this claim). Dest_diag did not run. Historical no-FREP smoke on this identity DEST_READ `0x0200010` = `fffffff7 ff7fffff 000070ea 00010000` (**not** magic). Same identity later produced magic only after FREP×3+FCMP. Leftover DRAM COMMIT from a prior compact is **CONTRADICTED** by that earlier beat.

### 3. Shadow state surviving FRST

FRST drives `fem_rst_n = rst_ui_n && !fm_rst_hold` for 8 UI cycles. That reset covers `fem_lifecycle`, `fem_t2_ce` (`dest_hold<=0`), adapter, and `fem_req_ui`. It does **not** reset MIG, dest_diag, or pack CDC.

FOBS_AFTER_FRST is bit-identical to FOBS_VIRGIN (`s0=0000380f` life=7 key=0 compacted=0). If C volatile / `dest_hold` had survived, FOBS would not be virgin.

DEST_READ after FRST uses **dest_diag** (independent UI master), not `fem_t2_ce.dest_hold`.

**FACT:** Volatile FEM cleared. **FACT:** dest_diag re-read of two FEM beats was bit-identical. **INFERENCE (strong):** those beats are MIG-UI media, not a FEM-side hold register.

### 4. UART/host reconstruction

FOBS frames are DUT `s0..s4` after `464f4253`. Packing is `fem_ctrl_cdc`:

```text
s0 = {13'h0, integrity_fault, compacted, unresolved, recover_state,
      life_state, n_raw, cmp_result, txn_step}
```

Independent decode of raw `s0=0002180f` (post-FCMP): txn=F cmp=0 n_raw=0 life=3 recov=0 compacted=1.  
Independent decode of raw `s0=0002980f` (post-FREC): same plus recov=2 (`bit15`).

Host script does not invent those bits; it shifts DUT words.

### 5. Hidden DEST_POKE

Harness opcodes: CLEAR, FRST, FOBS, FING, FREP, FCMP, DEST_READ (`DRD1`+addr), FREC. No DPK. JSON recs have no poke address/data. **FACT:** DEST_POKE not in this dataset.

### 6. Address alias

`A_COMMIT=4` → `FEM_BASE+{4,2'b00}` = `0x0200010`. Observed DEST_READ of that beat puts magic in lane0, INDEX lane1, HDR2 lane2. Pack slots are `0` and `0x0100000`. **FACT:** no SLOT/FEM_BASE alias in this map.

Lane3 of `0x0200010` remains `00010000` (no `A_*` at word 7). Unused. Not a fail. Not COMMIT.

### 7. Recovery bypass

C `R_CLASS` sets `recover_state=2` only if `rd_commit==COMMIT_MAGIC` and prototype CRC valid (`rec_crc_valid`). Integrity fault path holds recover_state=0. Post-FREC: recov=2 integrity_fault=0 life=3 compacted=1 n_raw=0 key=0x70ea. COMMIT beat still magic after FREC.

**FACT:** classification matches COMMITTED_NEW from committed media + valid prototype, after a proven volatile wipe.

Post-FREC DEST_READ of `0x0200000` was **not** repeated. Prototype beat retention after FREC is **INFERENCE** from pre-FREC identical HDR beat plus COMMIT beat after FREC, not a fourth DEST_READ. Not a contradiction of COMMITTED_NEW.

### 8. Provenance mismatch

PROGRAM.txt SHA matches disk unique persist bit `1db38691…` programmed 2026-09-21T21:33:03+07 EOS HIGH. This run: no bitstream, no reprogram, unique FEM opcode plane (FREP/FOBS) answered. **INFERENCE (strong):** SRAM still that identity. **NOT FACT:** cryptographic live-SRAM readback. `PROGRAM.DONE=NA` unresolved. Do not rewrite as live-SRAM proof.

### 9. Incorrect CRC interpretation

C and this audit: CRC16 init `0xFFFF` poly `0x1021` MSB-first over `{p_w0,p_w1}`. Result `0x552e`. A_CRCW high mark `0xA5A5`. Match. Byte-wise `fem_commit_class` order is the same 8 bytes.

---

## Historical DEST_READ-first inference

No-FREP `UART_SMOKE.json` sha256 `822f8750d1471c2f24ff7c9291d77d6ca8572f7a5ce88f399ff278ea366cd367` remains append-only.

Replacement FIRST_DIVERGENCE for **that** dataset: `TEST_HARNESS_FREP_OMITTED`. Guard NOT_RESOLVED → `C_DONE` without `C_B0…C_COMMIT`. Missing `C0117ED0` expected.

Marked `SUPERSEDED_BY_CAUSAL_AUDIT` on D/watch T143303Z / T144000Z copies and `FEM_PERSIST_BOARD_CEILING.txt`. Bodies not deleted.

---

## Gaps that are not contradictions

1. Live SRAM hash not read during legal-compact.  
2. `PROGRAM.DONE=NA`.  
3. FRST ≠ red-board reset, USB unplug, or power removal.  
4. No DEST_READ of `0x0200000` after FREC.  
5. Identity `1db38691` unique top keeps SPEAR/Q* idle; `fem_feat` is FOBS-only. Downstream decision **NOT_TESTED**.  
6. Unused lane3 `00010000`.  
7. `fem_commit_class` not the silicon writer; ignore as an alternate COMMIT source.

---

## Verdict

```text
CONTRADICTION_FOUND=NO
FEM_PERSIST_LEGAL_COMPACT_BOARD_CANDIDATE=SUPPORTED
FEM_PERSIST_STORAGE_RECOVERY_RESEARCH=CLOSED_UNLESS_NEW_CONTRADICTION
FEM_PERSIST_PASS=NO
PROGRAM_PASS=NO
BOARD_PASS=NO
MIG_PASS=NO
TIMING_PASS=NO
PACK_ABI_24_24_PASS=NO
NEXT_RESEARCH=FEM_TO_SPEAR_QSTAR_CAUSAL_INFLUENCE
```

Do not rebuild a persist bit to repeat COMMIT/FRST/FREC. Do not edit C FEM RTL. Do not reopen MEMORY/T2/MIG from a later behavioral fail without a new first-divergence.
