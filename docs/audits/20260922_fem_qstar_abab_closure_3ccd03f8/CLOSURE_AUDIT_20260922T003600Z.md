# Closure audit — FEM→Q* A/B/A/B identity 3ccd03f8

LANGUAGE=EN
RUN_ID: 20260922T003600Z
AUDITOR: this turn. Does not inherit the run's own PASS language.
SCOPE: the five checks requested. Point 5 arrived cut at "candidate/ac"; this audit treats it as SPEAR idle and the candidate/action set.

```text
CONTRADICTION_FOUND=NO
C_RTL_EDIT=NO
REPROGRAM_THIS_AUDIT=NO
DEST_POKE=NO
BOARD_PASS=NO
PROGRAM_PASS=NO
FEM_PERSIST_PASS=NO
TIMING_PASS=NO
MIG_PASS=NO
PACK_ABI_24_24_PASS=NO
PROGRAM.DONE=NA
LIVE_SRAM_HASH=NOT_READ
```

Identity under audit: `3ccd03f80677607acfba6e45aab1fe05d6a8a99055224a244dbc3b7de275229d`

JSON: `UART_QSTAR_ABAB.json` sha256 `302c7bd46e9262c745c3ef66fdc20d08ac378c8cae618bccb21c220c38b1c76f`

## 1. Provenance

| Check | Result |
|---|---|
| Disk bit rehashed this turn | `3ccd03f8…` matches `BIT_SHA256.txt` and the JSON `SHA256` field |
| Build directory | `build_fem_qstar_causal` only. Synth Tcl reads `fem_qstar_causal/arty_a7_r2_top_m4_mig_candidate.sv` |
| Persist disk bit | still `1db38691…`. Not the file that was programmed |
| Labtools | `PROGRAM_BEGIN` names that bit and that SHA. End of startup HIGH |
| `PROGRAM.txt` | same SHA and path. It was written after the `PROGRAM.DONE` query aborted the Tcl session. `PROGRAM.DONE=NA` |

Binding is programmed file SHA plus End of startup HIGH plus the unique QTHW/QARM/QOBS plane. It is not a live SRAM readback.

No `44504B31` in the JSON. `dest_poke=NO`.

## 2. Invariants except the mux

In the causal top, not in C RTL:

- `exam=1`, `epsilon16=0`, `legal_mask=8'h03` are constants
- `upd_start=0`, `exec_valid=0`, `learn_reset=0`, `lfsr_we=0`
- `theta_we` is high only in `qstar_dec_uart` state `S_THW`
- The JSON has one `QTHW`, before arm A. None between A, B, A2, B2
- `feat_flat = {56'h0, qs_infl ? ft_s1 : 8'h00}`

`q_sel` is 2 on B and again on B2 with no second theta write. That is the evidence theta survived FRST and was still the same weight on the last arm. Theta bytes were not read back on the wire.

## 3. A2 discriminator

Record order after recovery: `QARM_B`, `QOBS_B`, `QARM_A2`, `QOBS_A2`, `QARM_B2`, `QOBS_B2`. No FRST, FING, FREP, FCMP, FREC, or QTHW in that window.

Independent decode of A2 word `00000200` / `00000017`:

- feat0 = 0
- ft = 2
- life = 3
- compacted = 1
- greedy = 0
- q_sel = 0

B2 repeats B: feat0 = 2, ft = 2, life = 3, compacted = 1, greedy = 1, q_sel = 2.

FRST clears `fem_rst_n` only. Q* uses `rst100_n`. A theta wipe would leave B2 at q_sel 0. B2 is q_sel 2. A2 is the mux off, not a hidden Q* reload.

## 4. Stale CDC / stale QOBS

`S_WAIT` writes `greedy_l` and `qsel_l` from the Q* ports in the same step that emits the QARM echo. The echo is not sent before `prop_done`. `prop_done` is a one-cycle pulse. The next QOBS is accepted only after that echo finishes and the UART returns to idle, so it reads the latch from the arm that just echoed.

Observed `q_sel` moves 0 → 2 → 0 → 2. A stuck latch would have repeated the previous arm.

`feat_l` is the mux value captured when `prop_start` is issued. `q_sel` is the Q* result captured at `prop_done`. They agree: feat 2 with q_sel 2, feat 0 with q_sel 0. `ft` in the QOBS word is the live synced byte, which stays 2 on A2 while feat0 is 0.

FEM had been idle since `FOBS_AFTER_FREC` before arm B. The 2FF snapshot is updated only while `fem_busy` is low. Nothing in the B→B2 window starts FEM, so the snapshot is not a mid-update tear.

## 5. SPEAR and the action set

SPEAR in this top: `q_start=0`, `cand_valid=0`, `cand_desc=0`, `w_flat=0`, `prof_wr_valid=0`. No UART opcode in this run feeds a candidate. The legal action pair is the constant mask `8'h03`, not a host-selected list.

## Verdict

The five checks do not contradict the scoped claim: on this identity, recovered `failure_total` through the D-only mux changed Q* greedy, and clearing only that mux restored greedy 0 while the recovered FEM fields stayed.

That remains a UART board candidate. It is not `BOARD_PASS`.
