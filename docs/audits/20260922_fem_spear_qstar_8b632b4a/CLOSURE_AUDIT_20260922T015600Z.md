# Closure audit — FEM SPEAR to Q* identity 8b632b4a

LANGUAGE=EN
RUN_ID: 20260922T015600Z
CONTRADICTION_FOUND: NO

```text
IDENTITY=8b632b4ac5317f6176cf62103b88bfcdf637b8d4dec495717db9615eea3d3ad7
BOARD_PASS=NO
PROGRAM_PASS=NO
FEM_PERSIST_PASS=NO
TIMING_PASS=NO
MIG_PASS=NO
PACK_ABI_24_24_PASS=NO
PROGRAM.DONE=NA
C_RTL_EDIT=NO
```

Synth reads `fem_spear_qstar/arty_a7_r2_top_m4_mig_candidate.sv`. End of startup HIGH.

## No SPEAR bypass

`feat_flat` is `{56'h0, feat_q}`. `feat_q` is set only when `sr_done && sr_ok`, and only from `rank0 == 0xB1`. `failure_total` still feeds the SPEAR delta. It is not the Q* feature. `prop_start` is `prop_from_rank`, which waits for that rank. The old Q* UART `prop_start` is not connected to `qstar_select`.

The frame is sent only after `saw_rank && prop_done`. A new command clears `saw_rank`, so a previous proposal cannot finish the new frame.

## Arms

| Arm | rank0 | feat0 | greedy | q_sel | base A/B | delta B | ft | life | infl |
|---|---|---:|---:|---:|---|---:|---:|---:|---:|
| OFF1 | `0xA1` | 0 | 0 | 0 | 127 / 0 | 0 | 2 | 3 | 0 |
| ON1 | `0xB1` | 2 | 1 | 2 | 127 / 0 | 256 | 2 | 3 | 1 |
| OFF2 | `0xA1` | 0 | 0 | 0 | 127 / 0 | 0 | 2 | 3 | 0 |
| ON2 | `0xB1` | 2 | 1 | 2 | 127 / 0 | 256 | 2 | 3 | 1 |

`legal_mask` is tied to `8'h03`. The UART word `00000003` is that same constant, not a read of Q* internal `mask_r`. No host field can change it. No `QARM` and no DEST_POKE in this capture. Theta addr 8 is written once at boot, not per arm.

## Verdict

The chain recovered FEM → SPEAR rank → rank-0 → Q* feature → Q* action holds for this identity. It is not yet an ASTRA decision. Do not rebuild `8b632b4a…` to repeat it.
