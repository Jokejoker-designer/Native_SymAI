# U33TAP nạp và capture SOF — 2026-09-20

FACT — Owner: “Cho phép nạp board đấy”. Nạp identity quan sát **U33TAP** `d448544f88f09d4c78990b08c2810928d00e9f319f0bf653966ddacdd8897d7f`, không U33 `ff399e0b…`, không H. End of startup HIGH 17:14:35 +07. `PROGRAMMED` CANDIDATE. `PROGRAM_PASS=NO`. `TIMING_PASS=NO`. U33OBS hop-log chưa có bitstream.

Lần nạp đầu Vivado `EXCEPTION_ACCESS_VIOLATION`; lần 2 PROGRAM_OK. Tcl `97_program_uart_r2_u33tap.tcl`. JTAG `210319BE776EA`. Sau nạp hw_server không còn; COM12 FTDI `210319BE776EB`.

## Cells

Nguồn: [CAPTURE.json](D:/FPGA/arty_d/UART_R2/results/U33TAP_CAPTURE_20260920/CAPTURE.json). Host không dummy trên NATURAL/DUP4.

| Cell | CLEAR | V-04 / RX | TAP |
|---|---|---|---|
| NATURAL | n=0 (T0 sau nạp) | không TX | — |
| NATURAL2 reopen | ACK | GOLD `010000a5` n=4 | NO_DUMP |
| DUP4 extra BEGIN rồi V-04 | ACK | MAG `0200015a` + 24 B dump, n=28 | TAP1; p0=`00800001` BEGIN |
| DUMMY open–close rồi V-04 | ACK | GOLD n=4 | NO_DUMP |

DUMP DUP4 raw sau MAG:

`54415031 01008000 80004e41 49310100 01000100 1a0108a1`

Words LE: TAP1 `31504154`, p0 `00800001`, p1 `414e0080`, p2 `00013149`, p3 `00010001`, meta `a108011a`.

XSim DUP4 kỳ vọng p1=`00800001` (BEGIN thứ hai). Byte stream silicon sau p0 là `80 00 4e 41` = nửa sau BEGIN + nửa đầu MAGIC `3149414e`, không phải word BEGIN đầy đủ.

FACT — Leftover BEGIN trên silicon TAP **đủ** MAG. p0 loader TAP = BEGIN.

INFERENCE — p1 TAP có thể (a) dump UART mất 2 byte giữa pw0/pw1, hoặc (b) word MAGIC lệch alignment thật (H2). Chưa chọn được chỉ từ UART dump. Không patch `pack_loader`.

FACT — Dummy-open **không** mute trên U33TAP lần này (GOLD). Mute U33 A/B/A không tái hiện trên TAP. `INSTRUMENTATION_PERTURB` cho class MUTE. MAG leftover inject **có** tái hiện.

NATURAL2 GOLD không dump: TAP không phá hop GOLD (đúng spec dump-sau-NAK).

## Stamps

PACK_ABI_24_24_PASS=NO. BOARD_PASS=NO. PROGRAM_PASS=NO. Overlay U33/H=NO. 1 GOLD ≠ 24 ABI. DUP4 inject ≠ MAG lịch sử tự nhiên.

## Next

U33OBS (log xuyên CLEAR, dump khi mute) vẫn cần bitstream riêng. Quyền nạp TAP **không** biến TAP thành U33. Frozen U33 `ff399e0b` trên đĩa không bị ghi đè.
