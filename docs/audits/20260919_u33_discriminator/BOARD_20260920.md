# Owner-authorized board diagnostic — 2026-09-20

FACT — Owner authorized bounded board UART use in this task. No programming was performed. COM12 enumerated FTDI serial 210319BE776EB; the port opened successfully. Raw TX/RX logs were preserved separately; no automatic retries or buffer resets were used.

| Observation | Evidence / scope |
|---|---|
| First session | Host write of CLEAR bytes 43475244 returned 4; 3-second RX window returned zero bytes. |
| Read-only JTAG | Device xc7a100t_0, cable 210319BE776EA; DONE pin/internal=1, EOS=1, PLL lock=1, configuration CRC error=0. |
| Second explicit control session after JTAG closed | Host CLEAR write returned 4; RX was exactly three bytes 6e6f00. |
| Prefix stages | Not sent: both sessions failed the prerequisite of exact four-byte CLEAR ACK c1ea50a5. |
| Current resident identity | UNKNOWN. Disk bit still hashes ff399e0bb9e6ff6c91caf3b769270b03ca9cfb2ea317035a5f3533031338a350. Old programming history is not live SRAM identity. |

INFERENCE — FPGA is configured, so an entirely unconfigured FPGA is not the explanation at the JTAG observation point. The configuration PLL flag does not prove MIG calibration, user-clock health or UART correctness. A three-byte reply does not establish that the expected U33 parser decoded CLEAR; no ASCII or opcode interpretation is assigned to it.

FACT — This run did not reach the proposed header discriminator, did not reproduce the historical MAG transaction and does not close the MAG root cause. It establishes a currently failing baseline before that experiment.

Prepared next action, NOT EXECUTED — `program_exact_u33.tcl` may reprogram only the existing hash-pinned U33 bit, on the exact A7 cable/device, into SRAM after separate owner approval. It creates a new record and leaves historical PROGRAM.txt, RTL and bitstream files untouched. This resets runtime state; no flash write or new bitstream build. The earlier permission explicitly covered UART without programming, so approval for this next mutation is pending.

Primary files:

- D:/FPGA/arty_d/UART_R2/results/U33_PREFIX_OWNER_20260920T125930.jsonl
- D:/FPGA/arty_d/UART_R2/results/U33_PREFIX_OWNER_20260920T130320_CONTROL.jsonl
- D:/FPGA/arty_d/UART_R2/results/U33_PREFIX_OWNER_20260920T125930_JTAG/status.log
- D:/FPGA/arty_d/UART_R2/results/U33_PREFIX_OWNER_20260920_BOARD_RESULT.json (summary and hashes)

FACT — All opened hardware targets and COM handles were closed; no hw_server/cs_server remained at the final process check. PACK_ABI_24_24_PASS=NO; BOARD_PASS not stamped; PROGRAM=NO this run; overlay=NO.
