# Native_SymAI public status — 2026-09-19

Owner snapshot. Not a PASS stamp.

```
PACK_ABI_24_24_PASS     = NO
BOARD_PASS              = NO
PROGRAM_PASS            = NO
MIG_PASS                = NO
TIMING_PASS             = NO
ASTRA_PASS              = NO
FE256_PASS              = NO
M2_PASS                 = NO
FEM_PERSIST_PASS        = NO
FINAL_PASS              = NO
COMMON_RUNTIME_FE256    = NOT_RUN
MIG0_BOARD_CAUSAL_CLASS = STILL_OPEN
MIG0_PATH_THIS_SEQUENCE = FAIL_XSIM_CLEAR1_BUSY (U32 dest-AND seq)
RAW_MIG_READY_USED_AS_QUIESCENCE = SEEN_THIS_SEQ (PASS_XSIM)
H1_CAUSAL_CLEAR1_ACK    = PASS_XSIM (PACKAGE-qsc A/B)
PACKAGE_QSC_MIG0_TXN1_P0_P15 = PASS_XSIM (t1_seen=ffff)
PACKAGE_QSC_GOLD1       = PASS_XSIM got=010000a5 mute=0
Q1_MIG_UI32_IDLE_AFTER_GOLD1 = YES (PACKAGE-qsc settle snapshot)
PACKAGE_QSC_CLEAR2      = PASS_XSIM ACK c1ea50a5
Q2_DEST_IDLE_AT_CLEAR2  = YES (PACKAGE-qsc)
MIG0_PATH_THIS_SEQUENCE = FAIL_XSIM_CLEAR1_BUSY (U32 dest-AND)
PACKAGE_QSC_MIG0_PATH_THIS_SEQUENCE = CLEAN (PASS_XSIM TB-forced dest ready)
PACKAGE_QSC_GOLD2       = PASS_XSIM got=010000a5 mute=0
Q3_CLEAR2_RESET_BEFORE_RETIRE = NO
Q4_GOLD2_SOURCE         = NEW_COMMIT
MISSING_APP_RDY_GATE    = CONTRADICTED_THIS_SEQ (qsc path)
U33_BOARD_THIS_SEQUENCE = FAIL_BOARD_P5_V04_R2_MAG (replug T1; prior exclusive was r3)
U33_CLEAR1              = ACK c1ea50a5 n=4 (board)
U33_FIRST_DIVERGENCE    = p5 V-04 r2 MAG 0200015a (replug T1 2026-09-19T20:11)
U33_LAST_EQUIVALENT     = p5 V-04 r1 GOLD 010000a5
U33_HOST_BEGIN_N        = 1 FACT every V-04 TX (python dup BEGIN CONTRADICTED)
U33_MAG_TOKEN           = load_reject R_BAD_MAGIC (0200015a) FACT packing
U33_FIVE_V04_BRAM_XSIM  = PASS_XSIM five GOLD 12083475 ns (not board MAG cell)
U33_MIG0_FIVE_V04_XSIM  = PASS_XSIM five GOLD dest=generated_mig0 bind=U33 $finish 12207195 ns V04_4 010000a5 p0=00800001 p1=3149414e (CONTRADICTS dest=mig0 5th MAG)
U33_BAUD115200_XSIM     = PASS_XSIM FIVE115_0..4 GOLD nosettle @115200; R2 n=0-retry then GOLD; GAP_B0 mute not MAG ($finish 234454805 ns)
U33_REPLUG_EXCLUSIVE    = FAIL_BOARD 2026-09-19T20:08 program HIGH sha ff399e0b; T0 V04 NONE n=0; T1 Phase4+r0+r1 GOLD then r2 MAG 0200015a begin_n=1
U33_DUP4_XSIM           = PASS_XSIM MAG 0200015a p0=p1=BEGIN (4-byte BEGIN dup sufficient)
U33_DUP16_XSIM          = PASS_XSIM 0200035a R_SCHEMA (64B prefix NOT board MAG)
U33_PROBE_DUMMY_V04     = FAIL_BOARD MAG 0200015a on first V-04 after CLEAR ACK + dummy 00010001 n=0 (2026-09-19T20:23)
U33_LEFTOVER_BEGIN_MAG  = PASS_XSIM CELL_A MAG 0200015a p1=BEGIN; C/D/E GOLD; B mute; board leftover source UNKNOWN
U33_PHANTOM_CDC_CLEAR   = PASS_XSIM negative: no leftover BEGIN after CLEAR; 5th GOLD without inject; ABI01 leftover GOLD
PACK24_TWO_CLASSES      = CLEAR1 BUSY (U32, CONFIRMED) then MAG R_BAD_MAGIC (U33 remaining)
OPTION_A_ON_SILICON     = U33; CONTRADICTED as Pack24 close
PROGRAM_PASS            = NO (U33 PROGRAMMED End of startup HIGH; D does not self-stamp)
```

Labels used here: **FACT** / **INFERENCE** / **HYPOTHESIS** / **UNKNOWN** / **REJECTED**.
XSim ≠ board. `mig_ui_bram` ≠ generated `mig0`. Identity H ≠ H-ILA-A ≠ H_OBS.

Related maps:

- [GitHub Issues](https://github.com/Jokejoker-designer/Native_SymAI/issues) — #1 tracker, #2 NOT PASS, #3–#7 OPEN, #8–#9 UNKNOWN, #10–#12 SOLVED (closed)
- [`PACK24_DATAFLOW_CLOCK_BUFFER_AUDIT.md`](PACK24_DATAFLOW_CLOCK_BUFFER_AUDIT.md) — AGENT_D hop/clock/buffer/qsc map; CLEAR1 class = quiescence semantic not clock-ratio; no RTL
- [`../results/arty_d/AUDIT_SNAPSHOT_20260919.md`](../results/arty_d/AUDIT_SNAPSHOT_20260919.md) — UART_R2 + OBS01 code hashes
- [`OPEN_CAUSES_STILL_UNKNOWN.md`](OPEN_CAUSES_STILL_UNKNOWN.md) — H9–H20 retries
- [`IDENTITY_TABLE.md`](IDENTITY_TABLE.md) — bit SHA256
- [`../results/arty_d/first_divergence_01/STATUS.md`](../results/arty_d/first_divergence_01/STATUS.md)

---

## NOT PASS (claim ceiling)

These stamps are **not** earned. Do not treat a candidate bit, UART smoke, or XSim GOLD as any of them.

| Stamp | Status | Why |
|---|---|---|
| `PACK_ABI_24_24_PASS` | **NO** | Need frozen identities + 24/24 CLEAR→V-04 GOLD n=4 + Pack24 run1/run2/fresh dest-complete through `pack_mig_bind`. Missing on board. |
| `BOARD_PASS` | **NO** | Silicon Pack still unclassified (UNSUP / NAK / MAG / mute / CLEAR BUSY / n=0). |
| `PROGRAM_PASS` | **NO** | Owner may JTAG-program. D does not self-stamp PROGRAM_PASS. End of startup HIGH ≠ PROGRAM_PASS. |
| `MIG_PASS` | **NO** | Board dest is generated `mig0`. XSim DUT used `mig_ui_bram`. |
| `TIMING_PASS` | **NO** | Candidate route WNS numbers exist. Not a timing sign-off. |
| `ASTRA_PASS` | **NO** | ASTRA still fail-closed SEARCH_INCOMPLETE on M4 UART smoke. |
| `FE256_PASS` | **NO** | `FE256_R1_REFERENCE_FREEZE` is reference, not product. Common-runtime FE256 not run. |
| `M2_PASS` | **NO** | Isolated query-posting XSim is not M2_PASS. |
| `FEM_PERSIST_PASS` | **NO** | FEM persist blocked until Pack is B-classifiable. |
| `FINAL_PASS` | **NO** | Goal open. |

---

## SOLVED / REJECTED (do not revive without new evidence)

Closed as *explanations of the whole board failure* or as *allowed product patches*. Evidence layer is named. SOLVED ≠ BOARD_PASS.

| ID | Claim | Verdict | Evidence layer |
|---|---|---|---|
| S01 | UART PHY is the unique Pack root | **REJECTED** | FACT: ACK/GOLD/BUSY tokens exist on U20/U30/U31 board/XSim. Host TX/RX *looks* unstable because dest hang and leftover classes are UART-identical. |
| S02 | AXI UART Lite as 24/24 fix | **REJECTED** | Policy + no evidence it repairs dest lifecycle. |
| S03 | MAG overlay as 24/24 fix | **CLOSED** | MAG CLOSED (U8). Do not reopen. |
| S04 | Pad / resync / extra-byte *product* guard on identity H | **REJECTED** | Owner: no pad/resync on H. |
| S05 | Python joined an extra TX byte as CLASS A sole root | **REJECTED** | H19 board `tx_n==tx_expect==132`. |
| S06 | BEGIN dropped during CLEAR ACK hold @115200 | **REJECTED** | H16 hold-overlap `n_drop=0`, `first_p=BEGIN`. |
| S07 | A-01 leftover sitting in BRAM UART explains H12 isolate mute | **REJECTED** | H12 A-01 XSim 6/6 CLEAR ACK. |
| S08 | 1 Mbps TB baud gap as UNSUP root | **REJECTED** | H16 115200 still NAK not UNSUP. |
| S09 | JP2 as sole CLASS A root | **REJECTED** | CLASS A after JP2 removed. |
| S10 | Pacing as sole root | **REJECTED** | V-03 SENTINEL at both rates. |
| S11 | H-ILA-A / H_OBS dump classifies identity H | **REJECTED** | Different bit SHA. |
| S12 | H20 is a silicon fix | **REJECTED** | Classifier only. |
| S13 | SETTLE (e.g. 2048) as 24/24 fix | **CONTRADICTED** | U28 exclusive: Phase4 GOLD then r0 V-04 n=0. |
| S14 | `qsc`-gated BEGIN as 24/24 fix | **CONTRADICTED** | U29 first V-04 n=0 after ACK. |
| S15 | Drop `dest_ui_rdy` from `dest_accept` (overlay A→B) | **FORBIDDEN** | Not a 24/24 path. Overlay guard. |
| S16 | Further UART FSM overlays (U33, fifo_flush-on-GOLD, etc.) | **STOP** | Drift from original project logic. UART not unique root. |
| S17 | OBS01 BRAM CLEAR→V-04→CLEAR→V-04 dest lifecycle | **BRAM_PATH_THIS_SEQUENCE = CLEAN** | PASS_XSIM only. Q1 YES, Q2 YES, Q3 NO, Q4 NEW_COMMIT. `lack_fell` is CLEAR reset of sticky `load_ack`, not a new txn. |
| S18 | GOLD2 on BRAM this seq is sticky-ack replay | **REJECTED for this BRAM seq** | BEGIN2 → S_COMMIT → `load_ack` rise → GOLD2. Deltas vs BEGIN2 snapshot. |
| S19 | CLEAR2 on BRAM this seq chops a live `mig_ui32` txn | **REJECTED for this BRAM seq** | Q3=NO; dest IDLE/out=0 at `debug_clear`. |
| S20 | AGENT_C synthesizable RTL rewrite / BRAM-force at approved size | **CLOSED policy** | C MODE ON_DEMAND. PRIMARY_QUEUE CLOSED. C_SCALE_GUARD: theta 64 / actions 8 / features 8 / K_HARD_MAX 9 / FEM N_RAW=4. |
| S21 | `MISSING_APP_RDY_GATE` (mux `p_rdy` owns U32 `pack_quiescent`) | **CONTRADICTED_THIS_SEQ** | PASS_XSIM OBS01-MIG0: `p_rdy=0` on all 186 csv rows while `qsc` still followed `mig0.app_rdy`. |
| S22 | UART 115200 / WAIT_AFTER_ACK=0 / r2 n=0-retry / first-byte MARK gap as U33 MAG root | **CONTRADICTED** | PASS_XSIM `tb_u33_baud115200`: five GOLD nosettle; r2-retry GOLD; gap = mute n_p=0 not `0200015a`. |

S17–S19 **do not** prove generated `mig0` or board. S21 is this mig0 XSim sequence only. Claim ceiling remains:

```
BRAM_PATH_THIS_SEQUENCE = CLEAN
MIG0_PATH_THIS_SEQUENCE = FAIL_XSIM_CLEAR1_BUSY
PACK_ABI_24_24_PASS     = NO
MIG0_BOARD_CAUSAL_CLASS = STILL_OPEN
```

---

## UNKNOWN (holes, not guesses)

| ID | Question | Status |
|---|---|---|
| U01 | `COMMON_ROOT` on identity H: legal CLEAR ACK + exact 132-byte A-01 → UNSUP / NAK_R02 / MAG / mute | **UNKNOWN** |
| U02 | Extra-byte SOURCE for CLASS A (FTDI / PHY / FPGA RX / none) | **UNKNOWN** |
| U03 | Identity H `first_pack_word` + `bix` (not on wire; ILA blocked on H netlist) | **UNKNOWN** |
| U04 | CLASS B sticky mute on H campaign vs H17 incomplete-pack+BUSY XSim | **UNKNOWN** (candidate in sim, not proven on H mute) |
| U05 | FIRST_DIVERGENCE of dest path after CLEAR2 / BEGIN2 (P0–P15) on generated `mig0` | **SPLIT**: U32 dest-AND seq still **NOT_REACHED** (died CLEAR1 BUSY). PACKAGE-qsc **CLEAN** PASS_XSIM: Q1 YES Q2 YES Q3 NO Q4 NEW_COMMIT GOLD2 `010000a5`. Board still UNKNOWN. |
| U06 | U32 exclusive first CLEAR after JTAG: BUSY then n=0 mix; no ACK/GOLD | **UNKNOWN** class vs leftover vs dest hang. Same **token** as XSim CLEAR1 BUSY; not proven same cycle-causal class on silicon. |
| U07 | Cell-F mux dest-busy: ACK then hang; CLEAR after release → delayed GOLD (UART-identical leftover) | **UNKNOWN** how much of board n=0 this is |
| U08 | Whether CLEAR2 BUSY after a *clean* P6–P10 dest txn is quiescence/`pack_quiescent` gating vs MIG half-transaction (`cmd_acc XOR wdf_acc`) | **SPLIT**: U32 dest-AND never reached CLEAR2. PACKAGE-qsc after clean GOLD1: CLEAR2 **ACK** `c1ea50a5` `dclr_busy=0` — CLEAR2 BUSY **CONTRADICTED_THIS_SEQ** on TB-forced qsc. Board UNKNOWN. |
| U09 | Does U31/U32 `pack_quiescent` AND of raw `mig0.app_rdy`/`app_wdf_rdy` cause CLEAR BUSY while client IDLE/out=0? | **H1_CAUSAL_CLEAR1_ACK** PASS_XSIM: PACKAGE-qsc CLEAR1/CLEAR2 **ACK**; dest-AND CLEAR1 **BUSY**. PACKAGE-qsc full seq **CLEAN** Q4 NEW_COMMIT. Board causal still **HYPOTHESIS**. |

---

## OPEN ISSUES (work still required)

Do **not** UART-overlay. Do **not** patch BRAM loader from the clean BRAM seq. Do **not** overwrite identity H, freeze DCPs, H_OBS, H19/H20, B/FE256 gold, or AGENT_C RTL.

| ID | Issue | Next decisive test |
|---|---|---|
| I01 | Pack 24/24 CLEAR→V-04 GOLD n=4 on board | Blocked on dest/mig0 causal class. Not another UART patch. |
| I02 | OBS01-MIG0 P0–P15 | U32 dest-AND seq: `FIRST_DIVERGENCE=CLEAR1_ACK` BUSY. PACKAGE-qsc A/B: **CLEAN** PASS_XSIM Q1 YES Q2 YES Q3 NO Q4 NEW_COMMIT GOLD2 `010000a5` `$finish` 4958414625 ps. TB-forced dest ready. Do not product-strip `dest_ui_*` yet. No UART/dest_accept overlay. Still not PACK_ABI. |
| I03 | Half-write CLEAR | `debug_clear` MUST NEVER occur while `cmd_acc XOR wdf_acc`. Flags: `partial_wr_cmd_only`, `partial_wr_wdf_only`. |
| I04 | U32 exclusive FAIL_BOARD CLEAR1 | Separate from OBS01 BRAM CLEAN. Bit sha256 `0df4de2e…`. PROGRAM_PASS=NO. |
| I05 | FEM persist media/recovery | Blocked until Pack is B-classifiable. `NEXT_MAIN_D_TASK` after Pack. |
| I06 | Common-runtime FE256 256/256 | `COMMON_RUNTIME_FE256_STATUS=NOT_RUN`. Do not hybrid-route to dedicated FE256 engine. |
| I07 | Ethernet | ON_HOLD. |

OBS01-MIG0 locked checkpoints (txn after CLEAR2 / BEGIN2):

| Pn | Must see |
|---|---|
| P0 | BEGIN2 accepted after CLEAR2 |
| P1 | `mem_cmd_valid && mem_cmd_ready` write |
| P2 | `app_en && app_rdy` (write command accepted) |
| P3 | `app_wdf_wren && app_wdf_rdy` (write data accepted) |
| P4 | only after P2+P3 enter readback |
| P5 | readback command accepted |
| P6 | `app_rd_data_valid` |
| P7 | lane readback matches `wdata_r` |
| P8 | `ui_outstanding` decreases |
| P9 | `mem_resp_valid` consumed by loader |
| P10 | loader outstanding → 0 |
| P11 | sentinel readback complete |
| P12 | `S_COMMIT` |
| P13 | new `load_ack` rise (not `lack_fell`) |
| P14 | new GOLD |
| P15 | settle: loader idle + ui idle + out=0 + status drained |

Notable branches: P2 without P3 = CMD/WDF handshake; P2+P3 without P5 = `S_WR→S_RD` / cmd ready; P5 without P6 = read-return/MIG/DDR; P6–P10 clean but CLEAR2 BUSY = quiescence/CLEAR gating, not MIG txn.

Q4 on MIG0 stays: BEGIN2_ACCEPT → snapshot counters → COMMIT2 → new `load_ack` rise → new `st_valid` rise → GOLD2. Do not use `lack_fell`.

U32 dest-AND mig0 seq did **not** match BRAM: it failed before V-04 (`FAIL_XSIM_CLEAR1_BUSY`). PACKAGE-qsc matched BRAM Q1 YES Q2 YES Q3 NO Q4 NEW_COMMIT on generated `mig0` in XSim only (`PACKAGE_QSC_MIG0_PATH_THIS_SEQUENCE=CLEAN`). Still not PACK_ABI_24_24_PASS. Product bind still ANDs dest_ui_*.

---

## CANDIDATE evidence (not stamps)

| Item | Layer | Not |
|---|---|---|
| Pack/ABI-24 DUT 24/24 dest-complete through `pack_mig_bind` + `mig_ui_bram` | PASS_XSIM | not PACK_ABI_24_24_PASS, not mig0, not board |
| OBS01 BRAM Q1–Q4 as above | PASS_XSIM | not mig0, not board |
| OBS01-MIG0 CLEAR1 BUSY; `qsc===app_rdy` idle/out0; `RAW_MIG_READY_USED_AS_QUIESCENCE=SEEN_THIS_SEQ` | PASS_XSIM | not board, not MIG_PASS, not PACK_ABI_24_24_PASS |
| PACKAGE-qsc A/B CLEAR1 ACK `c1ea50a5` vs U32 dest-AND BUSY; `H1_CAUSAL_CLEAR1_ACK` | PASS_XSIM | TB-only force dest ready 1; not product patch; not board |
| PACKAGE-qsc txn1 P0–P15 + GOLD1 `010000a5` mute=0 Q1 YES | PASS_XSIM | csv truncated; not PACK_ABI / MIG_PASS / board |
| PACKAGE-qsc CLEAR2 ACK `c1ea50a5` Q2 YES | PASS_XSIM | GOLD2 UART incomplete; TB-forced dest ready; not board |
| PACKAGE-qsc BEGIN2 P0–P15 + GOLD2 `010000a5` Q1 YES Q2 YES Q3 NO Q4 NEW_COMMIT `$finish` 4958414625 ps | PASS_XSIM | TB-forced dest ready; not PACK_ABI / MIG_PASS / board |
| M4+mig candidate programmed 2026-09-17; UART hop-1 StructuredResult fail-closed SEARCH_INCOMPLETE | UART_BOARD_SMOKE_CANDIDATE | not BOARD_PASS / ASTRA_PASS / MIG_PASS |
| Identity H programmed; CLEAR campaign pack_ok=7/11 then mute | UART_BOARD CANDIDATE | not PACK_ABI_24_24_PASS |
| FE256 freeze DCPs | REFERENCE_IMPLEMENTATION | not FE256_PASS; do not overwrite |
| AGENT_C OOC 100 MHz unplaced Q*/SPEAR YELLOW FEM GREEN | PASS_OOC structural | not TIMING_PASS / BOARD_PASS |
| U32 leftover+two V-04 XSim GOLD-then-next on BRAM | PASS_XSIM | U32 exclusive board FAIL CLEAR1 |
| U33 115200 nosettle five GOLD + r2-retry GOLD; gap mute | PASS_XSIM BRAM | not board MAG; leftover BEGIN / FTDI still UNKNOWN |
| U33 exclusive after replug 20:08–20:11 +07 | FAIL_BOARD MAG `0200015a` at p5 r2 (Phase4+r0+r1 GOLD); T0 V04 NONE; host begin_n=1 | not PACK_ABI; PROGRAM_PASS=NO; python dup BEGIN CONTRADICTED |
| U33 DUP4 vs DUP16 XSim | PASS_XSIM DUP4 MAG; DUP16 `0200035a` not MAG; LATE CLEAR BUSY | not board close; 64B FTDI prefix CONTRADICTED as MAG token |
| U33 leftover dummy probe 20:23 +07 | FAIL_BOARD MAG on first V-04 after dummy `00010001` n=0 | dummy did not MAG; leftover source still UNKNOWN |

---

## What this snapshot is not

- Not a root-cause close of Pack silicon.
- Not permission to overlay UART, `dest_accept`, or C RTL.
- Not PROGRAM_PASS / BOARD_PASS / PACK_ABI_24_24_PASS.
